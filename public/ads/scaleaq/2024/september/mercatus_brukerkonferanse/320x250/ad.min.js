let currentIndex = 0;
const hello = [
  'optimaliserings',
  'datainnsamlings',
  'analyse',
  'integrasjons',
  'samarbeids',
  'rapporterings',
  'fôr-oversikts',
  'produksjonskost',
  'dataflyt',
  'arbeids',
];

function randomIndex() {
  return ~~(Math.random() * hello.length);
}

window.setInterval(function () {
  // Increment the index
  currentIndex = (currentIndex + 1) % hello.length;

  // Update the text content
  document.getElementById('changing-text').textContent = hello[currentIndex];
}, 1000);
